# FastAPI 백엔드 환경 변수 예시
# 이 파일을 복사하여 .env 파일을 만들고 실제 값을 입력하세요
# cp .env.example .env

# NextAuth JWT 검증을 위한 시크릿 (프론트엔드 .env.local과 동일한 값 필수!)
# 생성 방법: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 또는: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# 데이터베이스 URL (선택사항)
# 기본값: sqlite:///./app.db (SQLite 사용)
# PostgreSQL 사용 시: postgresql://user:password@host:5432/dbname
DATABASE_URL=sqlite:///./app.db

# 디버그 모드 (개발 시 true, 프로덕션에서는 false)
DEBUG=true
