# 프로젝트 구조 가이드

## 📁 디렉토리 구조

```
ai_designlab_website_ver4/
├── app/                          # Next.js App Router
│   ├── admin/                    # 운영자 관리 콘솔
│   │   └── page.tsx             # REQ6 기반 통합 관리 페이지
│   ├── playground/               # 메인 애플리케이션
│   │   └── page.tsx             # 116줄 - 섹션 컴포넌트 오케스트레이션
│   ├── help-requests/            # 도움 요청 페이지
│   │   └── page.tsx
│   ├── layout.tsx                # 루트 레이아웃
│   ├── page.tsx                  # 랜딩 페이지 (Aurora)
│   └── globals.css               # 전역 스타일
│
├── components/                   # 재사용 컴포넌트
│   ├── common/                   # 공통 UI 컴포넌트
│   │   ├── TabButton.tsx        # 탭 버튼
│   │   ├── FilterButton.tsx     # 필터 버튼
│   │   ├── WriteButton.tsx      # 글쓰기 버튼
│   │   └── SectionHeader.tsx    # 섹션 헤더
│   │
│   ├── sections/                 # 페이지 섹션 컴포넌트
│   │   ├── HomeSection.tsx      # 홈 미리보기
│   │   ├── PlayBookSection.tsx  # PlayBook 탭
│   │   ├── PlayDaySection.tsx   # PlayDay 탭
│   │   └── ACECommunitySection.tsx  # ACE 커뮤니티 탭
│   │
│   ├── AuroraBackground.tsx      # 랜딩 배경
│   ├── TypewriterSimple.tsx      # 타이핑 효과
│   ├── NoticeBanner.tsx          # 공지 배너
│   ├── GalleryCard.tsx           # 갤러리 카드
│   ├── GalleryModal.tsx          # 갤러리 상세 모달
│   ├── HelpButton.tsx            # 도움 요청 버튼
│   ├── HelpModal.tsx             # 도움 요청 모달
│   └── WritePost.tsx             # 게시글 작성 모달
│
├── data/                         # 데이터 관리
│   └── mockData.ts              # 목업 데이터 (GalleryItem 등)
│
├── types/                        # TypeScript 타입 정의
│   └── index.ts                 # 공통 타입 (GalleryItem, Tab, Role 등)
│
├── lib/                          # 유틸리티 함수
│   ├── constants.ts             # 앱 상수 (색상, 우선순위 등)
│   └── utils.ts                 # 헬퍼 함수
│
├── config/                       # 설정 파일
│   ├── landing.ts               # 랜딩 페이지 설정
│   └── requirements.ts          # REQ 리스트 정의
│
├── .cursorrules                 # 코딩 표준 및 규칙
├── .gitignore                   # Git 제외 파일
├── package.json                 # 프로젝트 설정
├── next.config.js               # Next.js 설정
├── tsconfig.json                # TypeScript 설정
├── tailwind.config.ts           # Tailwind CSS 설정
└── postcss.config.js            # PostCSS 설정
```

## 🎯 주요 설계 원칙

### 1. **단일 책임 원칙 (Single Responsibility)**
- 각 컴포넌트는 하나의 명확한 목적만 수행
- 예: `TabButton`은 탭 UI만, `FilterButton`은 필터 UI만 담당

### 2. **관심사의 분리 (Separation of Concerns)**
```typescript
// ✅ 좋은 예: 역할별 분리
data/mockData.ts      // 데이터
types/index.ts        // 타입
lib/constants.ts      // 상수
components/           // UI
```

### 3. **DRY (Don't Repeat Yourself)**
- 반복되는 패턴을 컴포넌트/함수로 추출
- 예: 탭 버튼 → `TabButton` 컴포넌트
- 예: 섹션 헤더 → `SectionHeader` 컴포넌트

### 4. **확장 가능한 구조**
```typescript
// 새 섹션 추가 시
1. components/sections/NewSection.tsx 생성
2. app/playground/page.tsx에서 import
3. activeTab에 타입 추가
```

## 📊 데이터 흐름

```
mockData.ts (중앙 데이터)
    ↓
sections/ 컴포넌트 (데이터 소비)
    ↓
common/ 컴포넌트 (UI 렌더링)
```

## 🔧 주요 기능 매핑

### 사용자 페이지
| 경로 | 파일 | 설명 |
|------|------|------|
| `/` | `app/page.tsx` | 랜딩 페이지 (Aurora) |
| `/playground` | `app/playground/page.tsx` | 메인 앱 (Home/PlayBook/PlayDay/ACE) |
| `/help-requests` | `app/help-requests/page.tsx` | 도움 요청 목록 |

### 운영자 페이지
| 경로 | 파일 | REQ | 설명 |
|------|------|-----|------|
| `/admin` | `app/admin/page.tsx` | REQ6.* | 통합 관리 콘솔 |

#### 관리 콘솔 탭
1. **대시보드** (REQ6.7): CTR, 참여율, 다운로드 통계
2. **권한 관리** (REQ6.3): R1/R2/R3 멤버 관리
3. **콘텐츠 관리** (REQ6.2): 대표작 편성, 배너 관리
4. **회차 운영** (REQ6.4): PlayDay 회차 생성/종료
5. **로그 조회** (REQ6.5): 다운로드/삭제 감사 로그
6. **태그 관리** (REQ6.6): 표준 태그 추가/비활성화
7. **요구사항**: REQ 리스트 전체 조회

## 🎨 스타일 가이드

### 색상 시스템 (`lib/constants.ts`)
```typescript
COLORS = {
  primary: '#87CEEB',        // Sky Blue
  primaryLight: '#B0E0E6',   // Light Blue
  accent: '#4A90A4',         // Accent Blue
  // ...
}
```

### 컴포넌트 네이밍
- **페이지**: `*Page` (예: `AdminPage`)
- **섹션**: `*Section` (예: `HomeSection`)
- **공통 UI**: 명사형 (예: `TabButton`, `GalleryCard`)
- **모달**: `*Modal` (예: `GalleryModal`)

## 🚀 확장 가이드

### 새 기능 추가 시
1. **타입 정의**: `types/index.ts`
2. **데이터 정의**: `data/mockData.ts` (필요시)
3. **컴포넌트 생성**: `components/sections/` 또는 `components/common/`
4. **페이지 통합**: 해당 페이지에서 import

### 새 REQ 추가 시
1. `config/requirements.ts`에 요구사항 추가
2. `app/admin/page.tsx`에 해당 기능 구현
3. 필요시 새 탭 추가

## 📝 코딩 규칙 (`.cursorrules`)

1. **파일 크기**: 최대 300줄
2. **함수**: 단일 책임, 명확한 네이밍
3. **타입**: `any` 사용 금지
4. **주석**: 복잡한 로직에만 작성
5. **불필요한 코드**: 즉시 제거

## 🔄 리팩토링 체크리스트

- [ ] 중복 코드 제거 (DRY)
- [ ] 큰 컴포넌트 분리 (< 200줄)
- [ ] 타입 정의 명확화
- [ ] 사용하지 않는 import 제거
- [ ] 일관된 네이밍 적용

## 📦 의존성 관리

### 핵심 라이브러리
- Next.js 14.2.0
- React 18.3.0
- TypeScript
- Tailwind CSS 3.4.0

### 추가 시 고려사항
- 번들 크기 최소화
- Tree-shaking 지원 여부
- 유지보수 활성도

## 🎯 성능 최적화

1. **코드 분할**: 섹션별 컴포넌트 분리로 자동 분할
2. **메모이제이션**: `useMemo`로 필터링 최적화
3. **이미지 최적화**: Next.js Image 컴포넌트 사용 (필요시)
4. **번들 최적화**: 불필요한 라이브러리 제거

## 🔐 보안 고려사항

1. **권한 체크**: URL 직접 접근 시 권한 검증 필요
2. **입력 검증**: 사용자 입력 sanitization
3. **로그 보관**: 5년 정책 준수 (REQ0.3)
4. **다운로드 제한**: 사내 로그인 전용 (REQ0.14)

---

**마지막 업데이트**: 2024-02-09  
**버전**: 3.5
