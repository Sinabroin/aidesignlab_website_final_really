# ===========================================
# 인증 (Authentication)
# ===========================================
# Provider: dev | proxy_header | oidc | saml
# - dev: 개발/테스트용 (쿠키 기반 간단 로그인)
# - proxy_header: 사내 프록시/게이트웨이 헤더 기반 인증
# - oidc: OAuth 2.0 / OpenID Connect (Entra ID 등)
# - saml: SAML 2.0
AUTH_PROVIDER=dev

# 개발 시 미들웨어 인증 우회 (true 시 /community, /admin, /playground 등 보호 경로도 인증 없이 접근)
# 프로덕션에서는 설정하지 말 것
# AUTH_BYPASS_MIDDLEWARE=true

# 세션 비밀키 (프로덕션에서는 반드시 변경!)
# 생성 방법: openssl rand -base64 32
SESSION_SECRET=change_this_to_a_long_random_string_in_production

# === 프록시 헤더 Provider (AUTH_PROVIDER=proxy_header 시) ===
# PROXY_LOGOUT_URL=https://autoway.hdec.co.kr/logout

# === NextAuth + Email Magic Link (Resend SMTP 권장) ===
# NEXTAUTH_URL=http://localhost:3001
# NEXTAUTH_SECRET=openssl rand -base64 32 로 생성
# ALLOWED_EMAIL_DOMAINS=hdec.co.kr (현대건설 이메일 도메인, 쉼표로 여러 개 가능)
# EMAIL_FROM=AI 디자인랩 <onboarding@resend.dev>
# EMAIL_SERVER_HOST=smtp.resend.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=resend
# EMAIL_SERVER_PASSWORD=re_xxxxxxxxxxxxxxxxxxxxx

# === SAML Provider (AUTH_PROVIDER=saml 시) ===
# SAML_ENTRY_POINT=https://autoway.hdec.co.kr/saml/sso
# SAML_ISSUER=your-issuer-id
# SAML_CERT=your-certificate

# ===========================================
# 데이터베이스 (Database)
# ===========================================
# Azure SQL Database 연결 문자열 (docs/AZURE_SQL_SETUP.md 참고)
# 미설정 시 mockData 사용
# DATABASE_URL="sqlserver://SERVER.database.windows.net:1433;database=DBNAME;user=USER;password=PASSWORD;encrypt=true"

# ===========================================
# 시크릿 관리 (Secrets)
# ===========================================
# Azure Key Vault URL (선택, Azure 승인 후 설정)
# KEY_VAULT_URL=https://your-keyvault.vault.azure.net/

# ===========================================
# FastAPI 백엔드 (Backend API)
# ===========================================
# FastAPI 서버 URL (프론트엔드에서 백엔드 API 호출 시 사용)
# NEXT_PUBLIC_API_URL=http://localhost:8000
